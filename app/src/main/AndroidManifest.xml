<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Team25"
        tools:targetApi="31">
        <activity
            android:name=".ui.reservation.ReservationActivity"
            android:exported="false" />
        <activity
            android:name=".ui.reservation.AddCreditcardActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.status.ReservationCancelActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.status.ReservationCheckReportActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.status.ReservationStatusActivity"
            android:exported="false" />
        <activity
            android:name=".ui.reservation.ReservationPaymentActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.companion.LiveCompanionActivity"
            android:exported="false" />
        <activity
            android:name=".ui.login.LoginEntryActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Redirect URI: "kakao${NATIVE_APP_KEY}://oauth" -->
                <data android:host="oauth"
                    android:scheme="kakao${kakaoApiKey}" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/CustomFontTheme"
            android:exported="false"/>
    </application>

</manifest>
